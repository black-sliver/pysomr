# Changelog

### next

* SoMRandomizer: update
  * Core: fix using embedded resources (actually generating output was broken in v1.48.0a1)
  * API: don't enable debug log by default
  * API: allow setting log output dir, debug log, spoiler log via OWSettings
  * Multiworld: disable receiving items while in boss encounter
  * Multiworld: fix Shade spells and Lumina spells setting too many attributes
  * Core: set NTR progression after Doom's wall (instead of incrementing it) in case something goes wrong
* Build: add universal2 wheel for macOS
* Build: update build tools

### v1.48.0a1

Initial test version